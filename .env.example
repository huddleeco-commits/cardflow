# CardFlow Environment Configuration
#
# For Railway deployment: Set these in Railway dashboard → Variables
# For local development: Copy to .env and configure

# Database (Railway provides this automatically)
# DATABASE_URL=postgresql://user:pass@host:5432/railway

# Authentication - REQUIRED
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secure-random-string-min-32-chars

# Environment
NODE_ENV=production

# Port (Railway provides this automatically)
# PORT=3005

# Anthropic API (optional - users can set their own in Settings UI)
# ANTHROPIC_API_KEY=sk-ant-api03-...
